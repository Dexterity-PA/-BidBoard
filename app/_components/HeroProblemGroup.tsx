'use client'

import { LayoutGroup } from 'framer-motion'
import type { ReactNode } from 'react'

export default function HeroProblemGroup({ children }: { children: ReactNode }) {
  return <LayoutGroup>{children}</LayoutGroup>
}
